#include <stdio.h>

int main(){
    int i,c,h;
    scanf("%d", &i);
    scanf("%d", &c);
 if (i>c){
    if (i>h){
        printf("%d", i);
    }
 }else{
    if (c>h){
        printf("%d", c);
    }
}
}